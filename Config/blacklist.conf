# GPU driver blacklist for passthrough
# File: /etc/modprobe.d/blacklist.conf

# Prevent host from using AMD GPU drivers
blacklist amdgpu
blacklist radeon

# Optional: Also blacklist NVIDIA if present
# blacklist nouveau
# blacklist nvidia